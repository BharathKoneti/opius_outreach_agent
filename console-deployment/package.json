{
  "name": "opius-console-cdk",
  "version": "1.0.0",
  "description": "CDK infrastructure for console.opiusai.com",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && cdk deploy --all --require-approval never",
    "destroy": "cdk destroy --all --force",
    "cdk": "cdk"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "aws-cdk": "^2.150.0"
  }
}